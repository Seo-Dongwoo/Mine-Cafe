import React from "react";
import "../../assets/css/Common/AuthButton.css";

const AuthButton = ({ content }) => {
  return <button className="auth-btn">{content}</button>;
};

export default AuthButton;
