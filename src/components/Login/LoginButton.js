import React from "react";
import "../../assets/css/Login/LoginButton.css";

const LoginButton = ({ content }) => {
  return <button className="login-btn">{content}</button>;
};

export default LoginButton;
